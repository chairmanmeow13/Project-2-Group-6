var scoreList = require('../data/scores.js');

module.exports = function(app){
  //a GET route that displays prev high scores?

  app.post('/api/scores', function(req,res){
    var newScore = //logic;
    var scoresArray = [];

    //logic to score module

      //push results into scoresArray
      scoresArray.push(newScore);
    }

};